// =============================================================================
// Outbound links that live outside the catalog
// =============================================================================

/**
 * "Share Your Feedback" — the survey every PDF resource page links to
 * (Jennifer's 8-11-26 PDF shell).
 *
 * Empty until Jennifer supplies the URL; the button hides itself rather than
 * shipping a link that goes nowhere, so dropping the URL in here is the only
 * change needed to turn it on everywhere.
 */
export const SURVEY_URL = '';
